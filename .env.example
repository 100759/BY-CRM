# 环境配置示例

## 本地开发环境

复制此文件为 `.env.local` 并填入你的配置值。

```
# Cloudflare Workers
CLOUDFLARE_ACCOUNT_ID=your-account-id
CLOUDFLARE_API_TOKEN=your-api-token

# JWT 配置
VITE_JWT_SECRET=dev-secret-key-change-in-production

# API 地址
VITE_API_BASE=http://localhost:8787/api
```

## 生产环境配置

在 `wrangler.toml` 中配置：

```toml
[env.production]
name = "crm-system-prod"

[env.production.vars]
JWT_SECRET = "your-production-secret-key"

[[env.production.d1_databases]]
binding = "DB"
database_name = "crm_db_prod"
database_id = "your-production-database-id"
```

## Cloudflare 账户信息

获取你的账户信息：

1. 访问 https://dash.cloudflare.com/
2. 右上角点击账户
3. 在 "API Tokens" 中创建新的 token
4. 选择 "Edit Cloudflare Workers" 模板
5. 复制 token 到环境变量

## 数据库配置

### 创建数据库

```bash
npx wrangler d1 create crm_db
```

### 获取数据库 ID

```bash
npx wrangler d1 list
```

### 在 wrangler.toml 中配置

```toml
[[d1_databases]]
binding = "DB"
database_name = "crm_db"
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
```

## 安全建议

1. **不要提交 .env 文件**：已在 .gitignore 中
2. **使用强密钥**：JWT_SECRET 应该是随机的强密钥
3. **定期轮换密钥**：定期更新 JWT_SECRET
4. **限制 API 访问**：在 Cloudflare 仪表板中配置 IP 白名单

## 本地开发快速设置

```bash
# 1. 复制环境文件
cp .env.example .env.local

# 2. 编辑 .env.local 填入你的配置

# 3. 启动开发服务器
npm run dev
```

## 常见配置问题

### Q: 如何找到我的 Cloudflare Account ID？

1. 登录 https://dash.cloudflare.com/
2. 右上角点击账户
3. 在左侧菜单找到 "Account ID"

### Q: 如何创建 API Token？

1. 登录 https://dash.cloudflare.com/
2. 右上角点击账户
3. 选择 "API Tokens"
4. 点击 "Create Token"
5. 选择 "Edit Cloudflare Workers" 模板
6. 配置权限和资源
7. 创建并复制 token

### Q: 如何重置 JWT 密钥？

编辑 `wrangler.toml` 中的 `JWT_SECRET`，然后重新部署：

```bash
npm run deploy
```

注意：更改 JWT_SECRET 会使所有现有的 token 失效，用户需要重新登录。
